# familytree
Family tree web app for family/internal use. The private tree is intended to be stored in the owner’s own Google Drive rather than a central backend, with support for invited family access and export to image/PDF.
